# Third-party notices

The repository does not vendor third-party software. Python dependencies named
in `pyproject.toml` and `environment.yml` are installed from their respective
distributions and retain their own licenses.

The MIT License applies to the original source code and documentation in this
repository. It does not relicense source datasets. Dataset accessions,
publications, acquisition routes, redistribution status, and unresolved terms
are recorded in `manifests/datasets.tsv`, `manifests/cohorts.tsv`, and
`docs/DATA_ACCESS.md`. Committed `source_data/*.csv` files are derived numeric
tables classified as redistributable for figure and table reproduction; raw
expression matrices and private identifier maps are not included.
